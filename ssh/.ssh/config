# Shared SSH config - managed in dotfiles
# Machine-specific hosts go in ~/.ssh/config.local

# Include machine-specific config FIRST (higher priority)
Include ~/.ssh/config.local

# Global defaults - MUST come last with Host *
Host *
    # macOS Keychain integration
    AddKeysToAgent yes
    IgnoreUnknown UseKeychain
    UseKeychain yes
    IdentitiesOnly yes
